# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

혼자 카드게임을 두려는 브라우저 방문자다. itch.io에서 링크를 열고, 로그인이나
설치 없이 그 자리에서 한 매치를 둔다. 상대 넷은 전부 AI다.

공개 출시를 목표로 하며, 대상은 한국어권과 영어권 방문자 양쪽이다. 두 언어를
동등한 1급으로 다룬다 — 영어는 번역 부산물이 아니라 같은 품질을 요구한다.

룰을 아는 사람과 모르는 사람이 섞여 들어온다. 정식 튜토리얼은 만들지 않되,
**규칙 요약과 용어 도움말 수준의 가벼운 안내는 제품 범위 안이다.** 모르는 사람이
첫 화면에서 무엇을 하는 게임인지 알 수 없으면 실패다.

현재 itch 배포는 Restricted(비공개) 상태이고, 공개 전환 시점은 미정이다.

## Product Purpose

서버 없이 도는 5인 마이티(Mighty) 카드게임이다. 단일 HTML 파일이 전부이고,
백엔드도 계정도 저장 서버도 없다.

성공은 두 축으로 잰다. **두 축의 비중은 동등하다.**

- **AI 기력** — 자가대전으로 학습한 신경망이 얼마나 강한가.
- **게임 완성도** — 사람이 혼자서도 계속 두게 되는가.

한쪽을 위해 다른 쪽을 깎는 결정은 이 프로젝트의 기본값이 아니다.

## Positioning

마스터 AI는 **사람 기보를 학습하지 않았다.** 자가대전으로 스스로 익혔고, 그
신경망이 브라우저 안에서 서버 왕복 없이 직접 돈다(onnxruntime-web 로컬 번들).

난이도는 3티어다. 중급·고급은 규칙 기반, 마스터는 v16e 신경망 네 좌석이며
선별된 국면에만 PIMC 탐색을 더한다. 티어 난이도는 h2h가 아니라 "네 좌석이 협력해
사람을 얼마나 이기는가"(기준 좌석 판당 상금)로 잰다.

스코어링은 「마이티리그 시즌6」 엑셀 수식을 그대로 이식했다. 완전 제로섬이며
주공 ×2(셀프 ×4) · 프렌드 ×1 · 야당 각 −1로 배분된다.

## Operating Context

- **배포**: itch.io HTML 프로젝트. 뷰포트 1000×700, Mobile friendly 체크,
  현재 Visibility=Restricted. 업데이트는 zip 교체로 URL을 유지한다.
- **로컬**: `npm run serve`(http://localhost:8080). `file://`로 직접 열면
  fetch가 막혀 **마스터 티어 모델을 못 불러온다** — 고급으로 폴백한다.
- **빌드**: `src/`를 고치고 `npm run build`로 `web/index.html`을 만든다.
  `web/index.html`은 산출물이라 직접 편집하면 다음 빌드에 덮어써진다.
- **한 세션의 모양**: 매치 단위(여러 라운드)로 두고, 라운드마다 비딩 →
  바닥패 교환 → 프렌드 지정 → 10트릭 → 정산으로 진행한다.

## Capabilities and Constraints

**확정된 기능**

- 난이도 3티어(중급·고급·마스터), 마스터 v16e 전좌석
- 룰 설정 22종 + 프리셋(마이티리그 룰 / 표준 룰)
- 치트 시트 — 진영별 필요 점수, 기루다 카운팅, 무늬별 잠재 탑카드 (단축키 `C`)
- 복기 — 전원 손패를 공개한 상태로 트릭 재생 (단축키 `R`)
- 되돌리기 — 비딩 1회 + 플레이 1회 (단축키 `Z`)
- 로그 내보내기 — 라운드/매치 단위 마크다운, 재현용 시드 포함
- 세팅(전승 확정) 자동 진행, 매치 누적 상금 차트
- 한국어 / English 전환
- 마스터 매치 A/B 플레이 경험 조사 — 로컬 저장, JSON 복사, 서버 전송 없음

**지켜야 하는 제약**

- **itch.io는 sandbox iframe이라 파일 다운로드가 막힌다.** 내보내기는 다운로드를
  시도하되 **항상 복사 가능한 창을 함께 띄운다.** 이 폴백을 제거하면 안 된다.
- 마스터 모델은 파일당 최대 16MB급이고, 모델이나 ort가 없으면 자동으로 고급으로
  폴백한다. **모델 없이도 게임은 끝까지 돌아가야 한다.**
- 모델은 현재 룰셋(리그 룰)에 맞춰 학습됐다. 관측에 룰 파라미터가 들어가지 않아
  최소공약이나 바닥패 귀속을 바꾸면 기력이 떨어진다(바닥패 야당 귀속 시 판당
  +514 → +287). 다른 지역 룰의 정식 지원은 관측 확장과 재학습이 전제다.
- 모델 파일은 내용이 바뀌면 반드시 개명한다(브라우저 캐시 — v6→v6b가 유래).
- 게임 엔진(`src/mighty-engine.js`)과 파이썬 포트(`training/mighty_engine.py`)는
  mulberry32 RNG까지 비트 단위로 동작이 같아야 한다.
- 사람 입력이 들어가는 모달은 비동기 경합을 전제로 `stateGen` 세대 카운터로
  무효화한다. 게임 모달과 내보내기 오버레이를 섞지 않는다.
- A/B 그룹은 매치 단위로 고정하고 플레이어에게 숨긴다. 응답에는 이름 등 개인정보를
  넣지 않으며, 외부 수집은 사용자가 복사한 JSON을 별도로 전달할 때만 이뤄진다.

**용어**

`docs/GLOSSARY.md`가 한/영 용어 확정본이다. 여당=Attackers, 야당=Defenders,
기루다=Trump, 바닥패=Kitty, 세팅=Claim, 주공=Declarer, 공약=Bid/Contract.
UI 문구를 새로 쓰면 한국어와 영어를 같이 넣는다.

**미결**

- itch 공개 전환 시점
- 모바일 레이아웃의 구체적 형태(현재 데스크톱 1000×700 기준으로 짜여 있다)

## Brand Commitments

- 이름은 **마이티 / Mighty**.
- 한국어와 영어는 동등한 1급 언어다.
- **AI 성향(승부사/밸런스/신중파)은 UI에 노출하지 않는다.** 매치 시작 시 좌석마다
  무작위 배정되고 매치 내내 고정된다. 성향이 드러나면 상대 비딩을 읽을 수 있어
  난이도가 흐려진다.
- 신경망 좌석에는 세대별 별칭이 붙는다(선견가·조율가·수문장·기억가·수련생).
  규칙 기반 좌석은 `규칙기반`으로 표시한다.
- 톤은 실측 우선이다. 이 프로젝트의 판단은 대부분 시뮬레이션 수치로 내렸고,
  대외 설명문도 그 수치를 그대로 보여주는 방식을 쓴다.

## Evidence on Hand

- `docs/itch-description.html` — 신경망이 수를 고르는 과정을 고등학교 수학
  수준으로 풀어 쓴 스토어 설명문(itch 새니타이저 제약 명시).
- `docs/slides/slide-01.png` ~ `slide-12.png` — 위 설명문의 도해 12종.
  `python3 tools/gen-nn-slides.py`로 재생성한다.
- `docs/CHANGELOG.md` — 버전별 결정과 그 근거 수치.
- `docs/devlog-guard-vs-search.md` — 가드 대 탐색 교사 증류 개발기.
- `docs/*.txt` 다수 — 인증·벤치마크 실측 로그(난이도 사다리, h2h, 수용 기준).
- `docs/training-architecture.svg`, `docs/nn-architecture.html` — 구조 도해.

**없는 것 — 지어내면 안 된다.** 플레이어 후기, 사용자 수, 플레이 통계, 매출,
수상 이력, 외부 리뷰, 제휴. 공개 출시 전이므로 실사용 데이터가 존재하지 않는다.

## Product Principles

1. **판단은 실측으로 한다.** 추측이나 한 판의 결과론이 아니라 페어드 실측과
   신뢰구간으로 결정한다. 발화 판 300건이 최소선이다.
2. **룰은 정본을 따른다.** 스코어링은 「마이티리그 시즌6」 수식이고 임의로 바꾸지
   않는다. 룰을 바꾸면 파이썬 포트와 파리티도 같이 간다.
3. **모델 없이도 게임은 끝까지 돌아간다.** 신경망은 강화이지 전제가 아니다.
4. **난이도는 감추고 실력은 드러낸다.** AI 성향은 숨기되, AI가 왜 그렇게 두는지는
   복기와 설명문으로 열어 준다.
5. **한국어와 영어를 같은 무게로 만든다.** 어느 쪽도 번역 부산물이 아니다.

## Accessibility & Inclusion

**목표: WCAG 2.2 AA.** 모바일은 데스크톱과 동등하게 지원한다.

현재 구현과의 격차가 크고, 향후 작업은 이 격차를 메우는 것을 포함한다.

- ARIA 속성이 **0개**다. 카드·좌석·트릭·정산 표에 접근 가능한 이름과 상태가 없다.
- `<html lang="ko">`가 고정이라 영어로 전환해도 언어 속성이 따라가지 않는다.
- 레이아웃이 itch 뷰포트 1000×700 기준이라 모바일 터치 타깃과 재배치가 미검증이다.
- `prefers-reduced-motion`은 이미 대응되어 있다(카드 애니메이션·강조 효과).
- 단축키 `C`·`R`·`Z`가 있으나 키보드만으로 전체 플레이가 가능한지는 미검증이다.
