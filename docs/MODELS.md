# ONNX 모델 레지스트리

마스터 티어 신경망의 버전별 특징과 이력. 배포 모델을 바꿀 때 이 문서를 갱신한다.

**관리 규칙**

1. 배포 모델 경로는 `src/ui.js`의 `MASTER_MODEL`이 단일 소스다.
2. **파일 내용이 바뀌면 파일명을 개명한다** (v6 → v6b 사례). itch.io는 같은 이름의
   파일을 브라우저가 캐시하므로, 같은 이름으로 내용만 바꾸면 구모델이 계속 로드된다.
3. 배포 게이트: `tools/accept-v4.js 900 advanced`(상금 +150·주공 승률 70%·전지적
   키카드 낭비 5% 미만·주공 딜미스 0) + `npm run bench` 티어 서열(마스터>고급>중급)
   + 구모델 페어드 대결(`tools/research/model_h2h.js`, 동등/우세 확인).
4. 내보내기는 `training/export_onnx.py` — logits+value 두 출력, JS/파이썬 argmax
   200/200 일치 검증 포함.
5. 학습 신호 승격은 **같은 딜 페어드 개입 인증**을 통과한 클래스만
   (`docs/TRAINING-PLAN.md` 검증 원칙 — 조건부 지표 단독으로는 승격 금지).

---

## 배포 이력 (web/model/)

| 파일 | 크기 | 관측 | 게임 버전 | 계보 | 상태 |
|---|---|---|---|---|---|
| mighty_master.onnx | — | 606 | ~v0.15 | 초기 PPO | 폐기(superseded) |
| mighty_master_v3.onnx | — | 688 | v0.16~ | 인코더 복원(606→688 정본) | 폐기 |
| mighty_master_v4.onnx | 16MB | 688 | v1.2.x | v4 정본 학습(5k 업데이트, 도메인 랜덤화) | 보관(비교 기준) |
| mighty_master_v5.onnx | 4MB | 739 | v1.3.0 | e2c005 — E2 관례 증류 conv=0.05 | 보관 |
| mighty_master_v6b.onnx | 6.5MB | 1630 | v1.4.0~현행 | b1attn — 어텐션 + conv=0.05 (+value 헤드) | **현행 배포** |

### v4 — 규칙 기반을 넘어선 첫 정본 (v1.2.x)

688차원 관측, PPO 자가대전. 고급(규칙 기반+협력 추론) 상대 판당 +365.
보조 헤드(suit16 보이드·trump4·friend6·win5)로 파트너 모델링 내장.
비딩은 휴리스틱 위임(v0.15.0 임시조치)이었다가 재학습으로 해제.

### v5 = e2c005 — 협력 관례 증류 (v1.3.0)

Phase E. 승률은 그대로 두고 사람이 읽는 협력 관례를 이식했다. 인증 3클래스
한정 CE 증류(conv=0.05): addL(아군 확정승 트릭 보태기, 트릭6+), feedP(야당
확정승 회피), sigW(주공 기루다 리드에 프렌드 점수 응답). 기루다 응답
23.4%→88.9%, 보태기 52.1%→72.2%, 은닉(관전자 식별 31.6%) 유지, 수용 상금
+446·주공 승률 78.3%로 기준선(v5000, +485·78.4%)과 사실상 동일.
conv=0.10(e2c010)은 관례 이득 동일(포화)에 승률 비용이 커서 탈락.

### v6b = b1attn — 어텐션 인코더 (v1.4.0~)

트릭 이력을 요약 통계 대신 **카드가 나온 순서 그대로**(트릭 토큰 11×81,
관측 739→1630) 소형 트랜스포머로 읽는다. 파이프라인 무변경(네트워크 내부
reshape), 앞부분 프리픽스 호환. 기력은 e2c005 동급이나 키카드 낭비 역대 최저
(마이티·조커 18.3%→13.6%, 넓은 정의 14.5%→11.5% — 고급 휴리스틱보다 낮음).
value 헤드 포함 export(logits+value) — v2.0 AI 복기의 승률 추정에 사용.
원명 v6이었으나 export 수정 시 캐시 무효화를 위해 v6b로 개명(규칙 2의 유래).

---

## 학습 산출물 (training/, 미배포 포함)

| 아티팩트 | 계보 | 결과 | 교훈 |
|---|---|---|---|
| base / base2 | 기준선 런 | — | 비교용 |
| a002 / a005 | Phase A λ 벌점 0.02/0.05 | 폐기 | 전지적 목표는 비학습성 — 판정 불일치로 낭비 오히려 증가 |
| alloc000/002/005 | Phase A alloc 벌점 | 폐기 | 같은 계열 실패 |
| v5000/v5002/v5010 | 관측 739 기준선·λ 라인 | v5000은 E2 비교 기준(+485) | λ 3값 전부 실패 → 경로 폐기 |
| e2c005 | conv=0.05 증류 | **v1.3.0 채택** | 인증 클래스 한정 CE는 승률 보존 |
| e2c010 | conv=0.10 증류 | 탈락 | 관례 이득 포화, 승률 비용만 증가 |
| b1attn (+b1attn_v) | 어텐션 + conv=0.05 | **v1.4.0 채택(=v6b)** | 순서 정보가 낭비를 줄임 |
| b2x | addL 트릭4+ 확장 증류 | 탈락 | 보태기 81.6% 달성했으나 낭비 7.14%>5% FAIL·동일시드 −108 — 재보정 게이트가 회귀 차단 |
| b3top | b1 재개 + trumpTop 4클래스 | **진행 중** (2026-08-07) | 후보 — 아래 참조 |

### b3top — 기루다 탑 리드 관례 (진행 중, 후보 v7)

인증(2026-08-07, 2,400시드 페어드): 주공 리드에서 정책이 기루다를 골랐고 내 최고
기루다 위 서열이 밖에 없으면 최고 기루다로 교체 → 발화 판 +166.8±113.7 유의 이득,
전체 +58.4±39.9, 승수 +15/645. 리드 무늬 선택까지 강제하는 변형(any)은 +69±260
중립이라 미인증 — 클래스는 "기루다를 내기로 한 결정의 서열 교정"에 한정.
b1 체크포인트 재개 +2,000 업데이트, conv=0.05 4클래스(addL·feedP·sigW·trumpTop).
게이트: v6b 페어드 대결 600판 동등/우세 + 수용 기준 + bench 서열.
v6b의 탑 리드 채택률 기준선 72.7% [8/11 소표본] — 증류 목표는 잔여 기회.

---

## 체크포인트 위치

- 워크트리 `training/ckpt_*` — ckpt_b1(=v6b 계보, update 2000), ckpt_b2x,
  ckpt_e2c005/010, ckpt_b3top(진행 중)
- 본 저장소 `training/ckpt_*` — v4 계열 구세대(ckpt_v5000/5002/5010, a002/a005,
  alloc*, base/base2)
- 재학습 지침·수용 절차는 `docs/HANDOFF.md`, 실험·인증 이력은
  `docs/TRAINING-PLAN.md`와 `docs/*-results.txt`
